package eth.abueide.necromancer.ecs.entities

import com.badlogic.ashley.core.Component
import eth.abueide.math2d.Vector2
import eth.abueide.necromancer.ecs.components.Player

/**
  * Created by Andrew Bueide on 12/18/17.
  */
class Necromancer extends Component {

//  def override updatePos(currentOrbitDegrees: Float, distanceFromCenterPoint: Float, centerPoint: Vector2): Unit = {
//    val radians = Math.toRadians(currentOrbitDegrees + offset)
//    val x = (Math.cos(radians) * distanceFromCenterPoint).toFloat + centerPoint.x
//    val y = (Math.sin(radians) * distanceFromCenterPoint).toFloat + centerPoint.y
//    pos = new Vector2(x,y)
//  }
}
